run:
  timeout: 5m
  tests: true

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - revive
    - unconvert
    - unparam

linters-settings:
  revive:
    rules:
      - name: var-naming
      - name: exported
        disabled: true
  goimports:
    local-prefixes: github.com/optimumsage/superkube

issues:
  exclude-use-default: false
  max-same-issues: 0
  max-issues-per-linter: 0
